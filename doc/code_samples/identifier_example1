program testindentifiers
    use al_coordinate_identifier
    implicit none
    integer :: i_phi

    ! Note: accessing as a variable like coordinate_identifier%phi is deprecated
    i_phi = coordinate_identifier_index('phi') 

    print *,i_phi
    print *,coordinate_identifier_index('phi')
    print *,coordinate_identifier_name(i_phi)
    print *,coordinate_identifier_description(i_phi)
end program
