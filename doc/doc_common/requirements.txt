# Sphinx and theme
sphinx >= 6.0, < 7.0
sphinx_immaterial >= 0.11.4, < 0.12

six  # un-listed dependency of sphinx-fortran

# Matlab domain
sphinxcontrib-matlabdomain

# Requirements for the HLI files we import for the Python docs
numpy
